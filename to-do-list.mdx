---
title: "To Do List"
---

Keeping this simple for now as I scaffold this up - Nizh Thunyakij

## The List

### Figure out how to get Shopify's data directly to a Sanity Project

- This is going to convoluted as we go further into this build but the basics we need to do is this.  We need these things → Collection Data and Product Data.
- The above should satisfy a basic model.

## Exploration Logs

<Update label="07/22/2026" description="Entering The Fray. Log by Nizh Thunyakij - Founder">
  ## Log One

  I've noticed that Shopify adds two things to Sanity that isn't native to Sanity. Both are due to directly connecting Sanity to the Shopify app obviously.

  What happens on Connect:

  1. A Sanity API Token is created with Editor Permissions
  2. A webhook is created. Uses HTTP POST request method. It says it's managed by the system? Will find out more about this in a bit. It specifically looks for this in the filter → `_type in ['product', 'productVariant', 'collection', 'home', 'settings'] && !delta::changedOnly((store))` . It triggers on all criteria. → Triggers on Create, Update, Delete. 

  But yeah that summarizes it. Our first goal will be creating these two things first and foremost. Will update later when I have more - End current log at 9:20AM.
</Update>
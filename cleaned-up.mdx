---
title: "Cleaned Up"
---

```typescript clean up navbar expandable
import {Suspense} from 'react';
import {Await, useAsyncValue} from 'react-router';
import {
  type CartViewPayload,
  useAnalytics,
  useOptimisticCart,
} from '@shopify/hydrogen';
import type {HeaderQuery, CartApiQueryFragment} from 'storefrontapi.generated';
import {useAside} from '~/components/NativeComponents/Aside/Aside';
import navbarStyles from './Navbar.module.css';
import { activeLinkStyle} from '../utilities';
import { Link } from '../Link';
import { CountrySelector } from '~/components/CountrySelector';
import { Menu, Search, User } from 'lucide-react';


interface HeaderProps {
  header: HeaderQuery;
  cart: Promise<CartApiQueryFragment | null>;
  isLoggedIn: Promise<boolean>;
  publicStoreDomain: string;

}




type Viewport = 'desktop' | 'mobile';

export function DefaultNavbar({
  header,
  isLoggedIn,
  cart,
  publicStoreDomain,
}: HeaderProps) {
  const {shop, menu} = header;
  return (
    <header className={`${navbarStyles.NavbarMainBackground} px-(--paddingX) z-101 sticky top-0 w-full h-15  flex flex-row items-center`} >


    <NavbarMenu
      shop={shop.name}
      menu={menu}
      viewport="desktop"
      primaryDomainUrl={header.shop.primaryDomain.url}
      publicStoreDomain={publicStoreDomain}
      />
      <NavbarCtas isLoggedIn={isLoggedIn} cart={cart} />
    </header>
  );
}

export function NavbarMenu({
  menu,
  primaryDomainUrl,
  publicStoreDomain,
  shop,
}: {
  shop:string;
  menu: HeaderProps['header']['menu'];
  primaryDomainUrl: HeaderProps['header']['shop']['primaryDomain']['url'];
  viewport: Viewport;
  publicStoreDomain: HeaderProps['publicStoreDomain'];
}) {


  const {close} = useAside();

  return (
    <nav className={` space-x-(--spacingX)  w-full flex flex-row items-center`}   role="navigation">

      <Link prefetch="intent" to="/" end>
        <span> {shop}</span>
      </Link>


        {(menu || FALLBACK_HEADER_MENU).items.map((item) => {
          if (!item.url) return null;
  
          // if the url is internal, we strip the domain
          const url =
            item.url.includes('myshopify.com') ||
            item.url.includes(publicStoreDomain) ||
            item.url.includes(primaryDomainUrl)
              ? new URL(item.url).pathname
              : item.url;
          return (
            <Link variant="nav"
              className={`md:flex hidden`}
              end
              key={item.id}
              onClick={close}
              prefetch="intent"
              style={activeLinkStyle}
              to={url}
            >
              {item.title}
            </Link>
          );
        })}



      </nav>
      
  );
}

function NavbarCtas({
  isLoggedIn,
  cart,
}: Pick<HeaderProps, 'isLoggedIn' | 'cart'>){
  return (

<>
{/**Mobile */}
<nav className={`items-center w-full md:hidden justify-end space-x-(--spacingX) flex flex-row`} role="navigation">


<div className="md:flex hidden">
         <CountrySelector />
</div>
        <Link variant="nav" prefetch="intent" to="/account" style={activeLinkStyle}>
          <Suspense fallback="Sign in">
            <Await resolve={isLoggedIn} errorElement="Sign in">
              {(isLoggedIn) => (isLoggedIn ? <User/> : <User/>)}
            </Await>
          </Suspense>
        </Link>
        <SearchToggle />
        <CartToggle cart={cart} />
                <NavbarMenuMobileToggle />

      </nav>

{/**Hidden */}
<nav className={`items-center w-full justify-end space-x-(--spacingX) hidden md:flex flex-row`} role="navigation">


<div className="md:flex hidden">
         <CountrySelector />
</div>
        <Link variant="nav" prefetch="intent" to="/account" style={activeLinkStyle}>
          <Suspense fallback="Sign in">
            <Await resolve={isLoggedIn} errorElement="Sign in">
              {(isLoggedIn) => (isLoggedIn ? 'Account' : 'Sign in')}
            </Await>
          </Suspense>
        </Link>
        <SearchToggle />
        <CartToggle cart={cart} />
                <NavbarMenuMobileToggle />

      </nav>

</>

  );
}

function NavbarMenuMobileToggle() {
  const {open} = useAside();
  return (
    <button
      className={`flex md:hidden`}
      onClick={() => open('mobile')}
    >
      <h3><Menu /></h3>
    </button>
  );
}

function SearchToggle() {
  const {open} = useAside();
  return (
    <button className={`${navbarStyles.reset}`} onClick={() => open('search')}>
      <Search/>
    </button>
  );
}



//Cart functions, the main export is CartToggle. Style the CartBadge 

function CartToggle({cart}: Pick<HeaderProps, 'cart'>) {

  return (
    <Suspense fallback={<CartBadge count={null} />}>
      <Await resolve={cart}>
        <CartBanner />
      </Await>
    </Suspense>
  );
}



function CartBanner() {
  const originalCart = useAsyncValue() as CartApiQueryFragment | null;
  const cart = useOptimisticCart(originalCart);
  
  return <CartBadge count={cart?.totalQuantity ?? 0} />;
}





function CartBadge({count}: {count: number | null}) {
  const {open} = useAside();
  const {publish, shop, cart, prevCart} = useAnalytics();

  return (
  
    <a
      className={` flex items-center`}
      href="/cart"
      onClick={(e) => {
        e.preventDefault();
        open('cart');
        publish('cart_viewed', {
          cart,
          prevCart,
          shop,
          url: window.location.href || '',
        } as CartViewPayload);
      }}
    >
<button className="relative" aria-label="my shopping bag" data-testid="cart" aria-expanded="false" aria-haspopup="dialog" aria-controls="minicart-flyout-container">
<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" strokeWidth="1.5" stroke="currentColor" className="size-6">
  <path strokeLinecap="round" strokeLinejoin="round" 
  d="M15.75 10.5V6a3.75 3.75 0 1 0-7.5 0v4.5m11.356-1.993 1.263 12c.07.665-.45 1.243-1.119 1.243H4.25a1.125 1.125 0 0 1-1.12-1.243l1.264-12A1.125 1.125 0 0 1 5.513 7.5h12.974c.576 0 1.059.435 1.119 1.007ZM8.625 10.5a.375.375 0 1 1-.75 0 .375.375 0 0 1 .75 0Zm7.5 0a.375.375 0 1 1-.75 0 .375.375 0 0 1 .75 0Z" />
</svg>

<span className="absolute inset-x-0 bottom-0.25 flex items-center font-semibold justify-center text-[0.6rem] " data-testid="cart-counter">                    {count === null ? <span>&nbsp;</span> : <span>{count}</span>}
            </span>
            </button>

    </a>
 
  );
}




 
```